/**
 * Kodebase CLI
 *
 * Entry point for the `kb` command that provides CLI utilities.
 *
 * @example
 * ```bash
 * # Initialize kodebase in a project
 * kb init
 *
 * # Start working on an issue
 * kb start A.1.1
 *
 * # Check status
 * kb status
 * ```
 */

// Import the CLI - this will auto-run
import "@kodebase/cli";
